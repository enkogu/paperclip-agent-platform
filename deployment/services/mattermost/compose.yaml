name: mte-mattermost
x-logging: &id001
  driver: json-file
  options:
    max-size: 10m
    max-file: '5'
services:
  mattermost:
    image: ${MTE_MATTERMOST_MATTERMOST_IMAGE:?required}
    environment:
      TZ: UTC
      MM_SQLSETTINGS_DRIVERNAME: postgres
      MM_SQLSETTINGS_DATASOURCE: postgres://${MATTERMOST_DB_USER:?required}:${MATTERMOST_DB_PASSWORD:?required}@mte-mattermost-postgres:5432/${MATTERMOST_DB_NAME:?required}?sslmode=disable&connect_timeout=10
      MM_BLEVESETTINGS_INDEXDIR: /mattermost/bleve-indexes
      MM_SERVICESETTINGS_SITEURL: ${MATTERMOST_SITE_URL:?required}
      MM_SERVICESETTINGS_LISTENADDRESS: :8065
    ports:
    - ${MTE_MATTERMOST_MATTERMOST_PORT_1_MAPPING:?required}
    volumes:
    - mattermost-config:/mattermost/config
    - mattermost-data:/mattermost/data
    - mattermost-logs:/mattermost/logs
    - mattermost-plugins:/mattermost/plugins
    - mattermost-client-plugins:/mattermost/client/plugins
    - mattermost-bleve:/mattermost/bleve-indexes
    networks:
    - mattermost-data
    healthcheck:
      test:
      - CMD
      - /mattermost/bin/mattermost
      - version
      interval: 30s
      timeout: 5s
      retries: 10
      start_period: 60s
    restart: unless-stopped
    stop_grace_period: 90s
    cpus: ${MATTERMOST_CPU_LIMIT:?required}
    mem_limit: ${MATTERMOST_MEMORY_LIMIT:?required}
    pids_limit: 512
    logging: *id001
volumes:
  mattermost-config:
    name: mte-mattermost-config
  mattermost-data:
    name: mte-mattermost-data-volume
  mattermost-logs:
    name: mte-mattermost-logs
  mattermost-plugins:
    name: mte-mattermost-plugins
  mattermost-client-plugins:
    name: mte-mattermost-client-plugins
  mattermost-bleve:
    name: mte-mattermost-bleve
networks:
  mattermost-data:
    name: mte-mattermost-data
    external: true
