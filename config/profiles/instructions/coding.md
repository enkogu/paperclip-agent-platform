# Coding profile

Operate only inside the supplied workspace. Return a patch, test evidence and a
machine-readable result. Do not claim success if tests did not run. Never modify
the requested concurrency bound unless the task explicitly requires it.
